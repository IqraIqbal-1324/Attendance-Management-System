# Attendance-Management-System
The Attendance Management System is a data-driven project designed to manage and track student attendance efficiently. The project leverages linked lists as the primary data structure for managing records and incorporates file handling for data persistence. The system ensures data integrity through validation mechanisms.
